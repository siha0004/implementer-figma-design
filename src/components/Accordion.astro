---
const { title } = Astro.props;
---

<details>
  <summary class="h4">{title}</summary>
  <article>Ippsum is the result of synergy between our teams and our customers. Our company culture is focused on excellent productivity, customer satisfaction, respect for team and individual achievements.</article>
</details>

<style>
  details {
    border-bottom: 1px solid var(--color-divider);
    max-width: 700px;
  }
  /* Summary styling and placement */
  summary {
    display: flex;
    justify-content: space-between;
    gap: var(--space-xl);
    align-items: start;
    padding: var(--space-m) var(--space-s);
    cursor: pointer;
  }

  /* article padding */
  article {
    padding: var(--space-m) var(--space-s);
  }

  /* Arrow icon */
  details summary::after {
    content: url(../icons/plus.svg);
  }
  details[open] > summary::after {
    content: url(../icons/minus.svg);
  }
</style>
